# Codex Meter 执行约束

本文件是本仓库的硬性执行规则。它不是建议，也不是可在实现过程中自行放宽的偏好。

## 0. 当前阶段：只整理配置目录和七表 schema

当前用户明确授权的任务只有配置文件归类和数据库结构归位：

- 使用根目录 `config/`，其中放置通用配置、API 美元价格配置、订阅 Credit 价格配置和七表 `schema.sql`。
- `schema.sql` 只包含：`source_jsonl`、`source_app_server`、`source_ccusage`、`usage_daily`、
  `usage_minute`、`usage_session`、`capacities_v2`。
- 允许修改的文件仅为 `config/` 下的配置占位文件、原 `config.json`/`schema.sql` 的归位、`.gitignore` 和本节规则。
- `archived/` 只读；不得恢复、复制、移动或删除其中的旧代码。
- 不得填充真实价格或运行参数，不得创建运行数据库，不得实现数据库访问、Pipeline、API、前端或启动逻辑。
- 不得执行迁移、采集、回填、编译、服务启动、Git 提交或发布。
- 完成条件：配置按职责归类、内容不丢失；`config/schema.sql` 仅含七张目标表及其必要索引；`.data/` 被 Git 忽略。

当前目标目录：

```text
config/{app.json,api-usd.json,subscription-credit.json,schema.sql}
src/{main.rs,config.rs,db.rs,pricing.rs}
src/pipelines/{mod.rs,source/{jsonl.rs,app_server.rs,ccusage.rs},result/materialize.rs}
src/service/{mod.rs,http.rs,report.rs}
web/index.html
scripts/codex-meter-service.sh
fixtures/.gitkeep
docs/.gitkeep
```

## 1. 唯一执行依据

实现必须严格按照以下顺序和优先级执行：

1. 用户当前明确要求；
2. `docs/MINIMAL_IMPLEMENTATION_PLAN.md`；
3. `docs/FINAL_DESIGN.md`；
4. 本文件；
5. `docs/DATA_SOURCE_REFERENCE.md` 和其他调研参考；
6. `docs/FINAL_EXECUTION_PLAN.md`、`docs/FINAL_EXECUTION_PLAN_REVIEW.md` 等历史计划。

`docs/FINAL_EXECUTION_PLAN.md` 和 `docs/FINAL_EXECUTION_PLAN_REVIEW.md` 已被最小实现计划取代，只保留为历史研究。`docs/DATA_SOURCE_REFERENCE.md`、`docs/RESEARCH_FINDINGS.md`、参考 HTML 和 ccusage 源码只能用于确认字段来源、界面细节和接口事实，不能自行改变产品目标、实施顺序或验收门禁。

如果代码现状与执行计划冲突，必须以计划为准；如果计划需要改变，先停止实现并向用户说明影响，得到明确确认后才能修改计划。

## 2. 开始任何改动前必须完成的检查

在写代码、改数据库、改页面或新增脚本前，必须先明确写出：

- 对应的计划章节、阶段和任务编号；
- 本次改动要解决的用户可见问题；
- 允许修改的文件和明确不修改的范围；
- 完成条件、测试方式和验收证据；
- 当前工作区已有改动，并确认不会覆盖或混入无关改动。

找不到明确的计划映射时，不得开始实现。必须先停下并请求确认，不能用“顺手补一下”“以后可能需要”作为理由扩大范围。

## 3. 严格禁止的执行行为

- 不得把自己的新想法、新页面、新字段、新数据源、新框架、新服务或新迁移加入当前任务，除非计划已有明确条目或用户明确批准。
- 不得因为某个技术实现更方便，就改变计划规定的页面信息架构、数据职责、算法口径或实施顺序。
- 不得把“能编译”“有数据库表”“有 API 路由”“有适配器骨架”“服务能启动”描述成业务功能完成。
- 不得在当前 M 阶段门禁未通过时进入下一阶段，也不得用外围运维工作掩盖核心页面、数据归因或算法未完成。
- 不得用默认值、零值、近似值或猜测填补缺失数据；应保留 `NULL`/质量标记并报告 pending、stale、unknown 或 blocked。
- 不得把独立验证器 ccusage 变成本机生产账本，不得让它替代 JSONL 主事实或 App Server 官方账号事实。
- 不得为了“看起来完整”继续堆叠 UI 字段；如果页面结构与参考页面或最终设计不符，必须重做页面契约和组件，而不是继续打补丁。
- 发现当前任务之外的问题时，只记录为 out-of-scope，不得擅自修复。

## 4. 阶段门禁

阶段必须按最小实现计划顺序完成，每个阶段都要通过事实、聚合、API、页面和测试的闭环门禁后才能进入下一阶段：

```text
数据事实 → 归一化/归类 → report 聚合 → API 契约 → 页面展示 → 测试与验收
```

特别要求：

- 历史账号分类、历史 JSONL quota、Reset 分段和 ccusage 双源对账未闭合前，不得声称页面一完成。
- 页面一未达到参考页面的日历、趋势、卡片、模型、Session 和审计信息要求前，不得开始扩展页面二的边缘功能。
- 页面二只能使用真实 Reset 窗口和真实 Credit 证据，不能继续以手工输入框作为最终实现。
- 页面三只实现最小计划规定的计算说明；设置中心、诊断平台、导出和备份属于计划外，不得擅自加入。

## 5. 计划追踪和证据要求

每次实现后必须维护计划追踪记录，至少能回答：

| 计划任务 | 实现文件 | 测试/fixture | 页面或 API 验收 | 当前状态 |
|---|---|---|---|---|

状态只能使用：`未开始`、`进行中`、`已验证`、`阻塞`、`计划外未执行`。只有存在可复现的测试、真实数据或页面验收证据时，才能使用 `已验证`。

参考页面 `/Users/Lendfating/Desktop/codex-usage-report.html` 是页面一的 golden reference。页面一的每次重大改动都必须逐项对照它，并同时检查 `FINAL_DESIGN.md` 中额外要求的官方窗口、Reset、本机/账号/未观测和双源验证。

## 6. 发现范围冲突时的处理

遇到以下任一情况，必须暂停当前实现并报告，不得自行裁决：

- 需求需要修改计划目标或扩大数据来源；
- 需要删除或重写可能包含用户数据的数据库迁移；
- 现有数据不能支持计划要求的计算；
- 参考页面与最终设计在公式或语义上冲突；
- 需要引入新的依赖、框架、后台服务或外部网络能力；
- 需要提交、推送、发布或进行其他外部状态变更。

报告必须说明：冲突事实、受影响的计划条目、可选方案、推荐方案和等待用户确认的具体问题。

## 7. 汇报和 Git 纪律

- 开始工作时先报告当前计划任务和非目标；持续工作期间不得悄悄换题。
- 汇报必须区分“已验证完成”“部分实现”“骨架存在”“未实现”，不得用模糊的“基本完成”。
- 每次声称完成必须附测试命令、数据范围、页面/API 检查或明确说明没有证据。
- 未经用户明确要求，不提交、不推送、不创建 PR，不重写或丢弃用户已有改动。
- 如果需要改变执行计划，先修改计划并等待确认，再修改代码；不能先改代码后补文档解释。
