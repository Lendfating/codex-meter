# ccusage contract

Phase 0 pins the locally observed compatible version to `20.0.19`. Production
invocations must use the Codex report and `--offline`; automatic, standard,
and fast speed modes are separate contract inputs. The exact JSON shape used
by the bridge is recorded in `contract.v20.0.19.json`.
