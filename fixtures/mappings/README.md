# Historical provider mappings

These rules are local, explicit, and reversible. Raw provider and raw plan
values remain the source facts; a mapping only supplies the display grouping
and classification source.

The first rule records the user-confirmed historical exception from the design:
`model_provider=pro` with `plan_type_raw=null` is shown as `Other/API`.
