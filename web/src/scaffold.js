/**
 * Phase 0 web shell marker.
 *
 * No product page is implemented until the later frontend phase.
 */
export const frontendScaffold = Object.freeze({
  name: "codex-meter-web",
  phase: 0,
  businessPagesImplemented: false,
});
